apiVersion: apps/v1
kind: Deployment
metadata:
  name: abysscore-gateway
  namespace: abysscore
spec:
  revisionHistoryLimit: 1
  replicas: 1
  selector:
    matchLabels:
      app: abysscore-gateway
  template:
    metadata:
      labels:
        app: abysscore-gateway
    spec:
      imagePullSecrets:
      - name: ghcr-secret
      containers:
      - name: gateway
        image: ghcr.io/bassmit12/abysscore-gateway:2e094979ab3a5c13f2a8bd3cd6a876c8003bd455
        imagePullPolicy: Always
        resources:
          requests:
            cpu: "50m"
            memory: "32Mi"
          limits:
            cpu: "200m"
            memory: "128Mi"
        env:
        - name: ENCORE_BASE_URL
          value: http://abysscore-backend:4000
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: otel-collector.tracing.svc.cluster.local:4317
        ports:
        - containerPort: 4001
---
apiVersion: v1
kind: Service
metadata:
  name: abysscore-gateway
  namespace: abysscore
  labels:
    app: abysscore-gateway
spec:
  type: NodePort
  selector:
    app: abysscore-gateway
  ports:
  - name: http
    port: 4001
    targetPort: 4001
    nodePort: 30001
