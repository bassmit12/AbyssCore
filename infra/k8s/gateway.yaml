apiVersion: apps/v1
kind: Deployment
metadata:
  name: abysscore-gateway
  namespace: abysscore
spec:
  revisionHistoryLimit: 1
  replicas: 1
  selector:
    matchLabels:
      app: abysscore-gateway
  template:
    metadata:
      labels:
        app: abysscore-gateway
    spec:
      imagePullSecrets:
      - name: ghcr-secret
      containers:
      - name: gateway
        image: ghcr.io/bassmit12/abysscore-gateway:1e3931d62063666dfae2cd63e89cfa14f24c30d8
        imagePullPolicy: Always
        resources:
          requests:
            cpu: "50m"
            memory: "32Mi"
          limits:
            cpu: "200m"
            memory: "128Mi"
        env:
        - name: ENCORE_BASE_URL
          value: http://abysscore-backend:4000
        ports:
        - containerPort: 4001
---
apiVersion: v1
kind: Service
metadata:
  name: abysscore-gateway
  namespace: abysscore
  labels:
    app: abysscore-gateway
spec:
  type: NodePort
  selector:
    app: abysscore-gateway
  ports:
  - name: http
    port: 4001
    targetPort: 4001
    nodePort: 30001
