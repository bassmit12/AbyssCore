PORT=4000

# Encore services base URL (when running via encore run)
ENCORE_BASE_URL=http://localhost:4001

# Keycloak (for token validation if gateway validates directly)
KEYCLOAK_ISSUER=http://localhost:8080/realms/abysscore
